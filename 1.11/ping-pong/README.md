# Ping pong

Web server that responds to GET /pingpong with "pong N", where N is an in-memory counter that increases on every request. The counter is stored in $FILE_PATH. Shares Ingress with the "Log output" application.